{% extends 'base.html.twig' %}

{% block title %}NftPrice{% endblock %}

{% block body %}
    <h1>NftPrice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nft_price.id }}</td>
            </tr>
            <tr>
                <th>PriceDate</th>
                <td>{{ nft_price.priceDate ? nft_price.priceDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EthValue</th>
                <td>{{ nft_price.ethValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nft_price_crud_index') }}">back to list</a>

    <a href="{{ path('app_nft_price_crud_edit', {'id': nft_price.id}) }}">edit</a>

    {{ include('nft_price_crud/_delete_form.html.twig') }}
{% endblock %}
